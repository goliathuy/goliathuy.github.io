This directory is the **production build** of `kegel-ui/` (Vite `npm run build` with `base: "/kegel/"`). Do not edit files here by hand; rebuild from `kegel-ui` and copy `dist/` contents into `kegel/`.

Includes **PWA** output: `sw.js`, `manifest.webmanifest`, and icons. See `kegel-ui/README.md`.
